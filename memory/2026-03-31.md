- 2026-03-31: Built and iterated a policy/official-document search system around a simulated-click workflow. Final high-level skill added: `skills/simulated-click-search-workflow/SKILL.md` to orchestrate the pipeline.
- 2026-03-31: Confirmed default user preference for this afternoon's searches: when user says “搜索…”, use the simulated-click search workflow instead of lightweight search.
- 2026-03-31: Updated workflow preference: after expansion, default to expansion option #1 automatically unless user explicitly overrides.
- 2026-03-31: Updated workflow preference: if step 5 extraction count is 0, reply exactly “暂未搜索到相关内容”.
- 2026-03-31: Final agreed search pipeline currently is: natural-language query -> expansion -> default choose #1 -> Safari/Baidu simulated click search with current-year date-range filter -> save result-page H5 to `~/Desktop/h5/` -> process H5 into `~/Desktop/处理文件夹/搜索结果.xlsx` + `原文链接.md` -> batch direct-fetch detail links into `~/Desktop/detail-h5/` -> mark poor/partial items for browser fallback.
- 2026-03-31: Step 4 formal desktop script is `~/Desktop/百度_Safari_H5抓取.command`; it now attempts Baidu time filtering by clicking “时间不限”, filling current-year start/end dates, clicking confirm, saving H5, and auto-closing the Safari page afterward.
- 2026-03-31: Step 5 skill is `search-h5-processor`; it now overwrites prior outputs, writes Excel with only four columns (`标题`, `简介`, `原始百度链接`, `原文链接`), and resolves Baidu redirect URLs to real government landing URLs when possible.
- 2026-03-31: Step 6 skill is `detail-link-fetcher`; it reads `~/Desktop/处理文件夹/原文链接.md`, fetches links serially with 1-second spacing, writes `~/Desktop/detail-h5/` Excel with fixed schema, and includes completeness columns (`抓取状态`, `正文长度`, `是否检测到附件`, `是否需要浏览器兜底`).
- 2026-03-31: User wants step 6 and future step 7 output filenames to follow “YYYYMMDD_HHMMSS + search keyword without domain”; current implementation partially applies this but filename keyword derivation from `原文链接.md` title is noisy and should later be improved by passing the original search term explicitly.
- 2026-03-31: Added/updated related skills during the session: `policy-search-fixed-workflow`, `browser-click-search`, `search-h5-processor`, `detail-link-fetcher`, `detail-page-capture`, and removed obsolete `policy-search-workflow-memory`.
