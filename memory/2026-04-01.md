
## 晚间补充（19:07）

### 扩词规则重大更新（本次修改）
- 省份词不再进入搜索关键词，只用于确定域名
- 搜索词 = 清洗后关键词（如"新能源"、"无人汽车"）
- 最终搜索词：`{关键词} site:{域名}`
- 年份字段格式改为 `2023-01-01,2026-12-31`（Safari 第3参数格式）

### Safari 脚本更新（capture_multi_page_baidu.command）
- 改为新标签页打开搜索（`make new tab`），不动当前窗口
- 新增第3参数 YEAR_RANGE，格式 `2023-01-01,2026-12-31`
- 搜索完成后直接关闭标签页，不留空白页

### fetch_detail_links.py 更新
- 新增第1参数 `<搜索关键词>`，决定输出文件名
- 输出命名：`{搜索关键词}_{YYYYMMDD_HHMMSS}.xlsx`

### 已同步的脚本（3处同步）
- `multi-page-simulated-click-search-test/scripts/`（主目录）
- `multi-page-simulated-click-search/scripts/`（锁定版）
- `detail-link-fetcher/scripts/`（相关技能）
- 桌面脚本：`~/Desktop/百度_Safari_H5抓取_多页.command`

## 下午补充（18:18）

### 扩词技能重大重构
- build_single_policy_query.py 完全重写，解决以下问题：
  - clean_query_text 把"搜索"变成"到"导致 region 识别失败
  - 年份区间格式支持（2023到2026年）
  - 省份简称→全称展开
  - "省"字去除逻辑
- 关键词策略：只有省份无其他词时加"政策"；有具体搜索词时保留原词不加"政策"
- 年份单独拎出：返回 `年份: "2026到2026"` 或 `"2023到2026"`，传给 Safari 时间筛选
- 输出格式：`{"年份","地区","域名锚点","清洗后关键词","最终搜索词","模式"}`

### Safari 脚本重构
- 桌面脚本重写为 Python 版（/tmp/safari_search.py），彻底解决 shell heredoc 引号转义问题
- 所有 JS 片段先写入文件，osascript 读取文件执行
- 新建标签不动当前窗口，搜索完成后关闭新标签
- 时间筛选通过 JS 注入（日期填充事件序列已与原始脚本一致）

### 多省搜索脚本
- multi_province_search.py 完整可用
- 支持年份区间传递（start_date, end_date）
- 扩词和 Safari 调用链路已打通
- 多省并行：每个省独立扩词→独立搜索→H5合并处理

### 已锁定的技能
- ~/.openclaw/workspace-search/skills/multi-page-simulated-click-search/
- checksum 已记录，文件锁定，后续修改需用户明确同意
- 同步记录：build_single_policy_query.py 在两个目录同步更新

### 飞书机器人配置
- openclaw.json 已写入 search 账号绑定
- accounts: default（原有机器人→main代理）+ search（新建机器人→search代理）
- bindings 已正确配置
- dmPolicy: allowlist，用户 open_id: ou_c7ef480b7bce39e9dcf7a469cb08190d
- 建议：飞书连接问题可参考 ~/.openclaw/extensions/openclaw-lark/skills/feishu-troubleshoot/SKILL.md

### exa-web-search-free 技能
- 已安装，mcporter 已装，exa MCP 已配置
- 测试可用：web_search_exa 正常工作

### agent-browser 技能
- 已安装 CLI（v0.23.4）+ Chrome headless
- 但不推荐用于百度搜索（accessibility tree 不暴露链接 URL）
- 适合通用浏览器自动化场景

### 搜索相关技能清单
- 正式版：multi-page-simulated-click-search（已锁定）
- 测试版：multi-page-simulated-click-search-test（调用外部技能）
- exa-web-search-free（API搜索）
- agent-browser（浏览器自动化）
- search-h5-processor（H5解析）
- detail-link-fetcher（原文抓取）
- policy-single-query-expand（扩词）
