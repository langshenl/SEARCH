---
name: browser-click-search
description: Safari 模拟搜索页抓取技能。用于处理“打开 Safari 搜百度”“输入搜索词并导出整个 H5 页面”“把百度搜索结果页保存到桌面 h5 文件夹”“用浏览器方式抓取完整页面源码”等请求。适用于需要： (1) 模拟打开 Safari 浏览器，(2) 打开百度搜索页并输入用户搜索词，(3) 打开开发者模式或等效方式获取当前搜索结果页完整 H5/HTML 源码，(4) 将页面源码保存到桌面 h5 文件夹中。
---

# Browser Click Search

## Overview

这个技能当前只做一件事：

**在用户已经从扩词结果中选定某一条搜索词后，模拟使用 Safari 打开百度，输入该条搜索词，进一步点击“时间不限”的时间筛选下拉框，并在两个输入框中填写本年的第一天和最后一天（格式：年-月-日），再把筛选后的搜索结果页完整 H5/HTML 页面源码保存到桌面 `h5` 文件夹。**

不要在这个技能里顺手做：
- 扩词
- 结果提取
- 去重清洗
- 点击多个结果继续深抓
- 政策库整理

先把“打开页面并保存整页源码”这件事单独做好。

## Fixed Workflow

### 1. 接收用户选中的搜索词

只使用用户从扩词结果中明确选中的那一条搜索词。

默认规则：
- 本技能不负责扩词
- 不自动补 `site:`
- 不替用户改写搜索词
- 进入本技能前，应已经完成“扩词 → 返回候选词 → 用户选中一条”这一步

### 2. 打开 Safari 并进入搜索结果页

使用 Safari 作为固定浏览器。

目标动作：
- 启动 Safari
- 打开百度
- 在百度搜索框输入用户搜索词
- 进入对应搜索结果页

### 3. 设置百度时间筛选

进入搜索结果页后，进一步尝试：
- 点击“时间不限”下拉框
- 找到自定义时间或等效时间筛选区域
- 在两个输入框中填写本年的第一天和最后一天
- 日期格式固定为：`年-月-日`

例如本年是 2026 年，则填写：
- 开始日期：`2026-01-01`
- 结束日期：`2026-12-31`

如果页面存在确认按钮、应用按钮或回车触发，则完成时间筛选提交。

如果当前页面没有可用的时间筛选控件，必须明确说明“页面未找到时间筛选控件”，不要假装已经筛选成功。

### 4. 获取当前搜索结果页完整源码

### 5. 获取当前搜索结果页完整源码

核心目标不是“截图”，而是：

- 获取当前搜索结果页完整 H5 / HTML 页面内容
- 尽量保存渲染后的当前页面源码或等效 HTML 内容

如果需要借助 Safari 开发者工具、页面另存、浏览器导出、自动化脚本等方式实现，可以使用；但最终目标始终是：

**得到当前搜索结果页对应的完整页面文件。**

### 6. 保存到桌面 h5 文件夹

输出目录固定为：
- `~/Desktop/h5/`

要求：
- 如果文件夹不存在，则创建
- 每次任务保存为独立文件
- 文件名应包含：时间戳 + 搜索词的安全文件名

推荐文件名格式：
- `YYYYMMDD_HHMMSS_搜索词.html`

例如：
- `20260331_100300_湖北省政策.html`

### 7. 输出说明

完成后返回给用户：
- 使用的搜索词
- 是否成功应用时间筛选
- 使用的起止日期
- 保存的文件完整路径
- 若失败，明确失败在哪一步（Safari 未打开 / 百度未加载 / 未找到时间筛选控件 / 页面源码未取到 / 文件写入失败）

## Hard Rules

- 这个技能当前只负责“打开百度搜索页、尝试设置本年时间筛选、并保存整页 H5/HTML 文件”。
- 不在本技能中自动扩词。
- 不在本技能中自动抽取标题、链接、摘要。
- 不在本技能中直接做政策清洗。
- 不要把“保存整页 HTML”和“解析搜索结果”混成同一步。
- 如果未找到“时间不限”下拉框或未找到日期输入框，必须明确报告，不得默默跳过后当作已完成时间筛选。

## Output Path

固定输出目录：
- `~/Desktop/h5/`

## Future Extension

后续如果要扩展，再考虑加：
- 从保存的 HTML 中提取搜索结果
- 判断页面是否为安全验证页
- 自动识别真实落地链接
- 作为其他搜索技能的前置抓取层

但当前版本先不要扩出去。
